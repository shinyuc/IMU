clc; clear; close all
label_1 = csvread('label_1.csv');
for i = 61:91
    M = csvread([num2str(i),'.csv'],1,1);
    M = M'; M = M(1:6,:);
    for j = 1:30
        index = label_1(j,1);
        mark = index*50;
           
    
end